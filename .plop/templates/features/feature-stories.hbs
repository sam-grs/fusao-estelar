import type { Meta, StoryObj } from "@storybook/react";
import { {{ pascalCase name }} } from "../{{ pascalCase name }}";

const story{{ pascalCase name }}: Meta<typeof {{ pascalCase name }}> = {
  title: "Features/{{ pascalCase name }}",
  component: {{ pascalCase name }},
};

export default story{{ pascalCase name }};
type Story = StoryObj<typeof {{ pascalCase name }}>;

export const Story{{ pascalCase name }}: Story = {
  args: {
    color: "red",
    text: 'Criando um parágrafo'
  },
};

